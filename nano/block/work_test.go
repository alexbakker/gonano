package block

import "testing"

func TestBlockWork(t *testing.T) {
	/*if !GenesisLive.Common.Work.Valid(GenesisLive.Root()) {
		t.Errorf("genesis block work not valid")
	}

	worker := NewWorker(GenesisLive.Common.Work, GenesisLive.Root())
	if worker.Generate() != GenesisLive.Common.Work {
		t.Fatal("work not equal")
	}

	worker.Reset()
	worker.Generate()*/
}
